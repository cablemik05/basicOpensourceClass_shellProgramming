#!/bin/bash

# 내부 함수 정의
run_command() {
    echo "함수 진입: 명령어를 실행합니다."
    
    # eval을 사용하여 전달받은 문자열을 명령어로 변환해 실행
    # "$*"는 전달된 모든 인자를 하나의 문자열로 합칩니다
    eval "$*"
}

echo "프로그램 시작"

# 스크립트 실행 시 입력한 인자들을 함수로 전달
# 예: ./ex3-5.sh ls -l -> "ls -l"이 함수로 넘어감
run_command "$@"

echo "프로그램 종료"
